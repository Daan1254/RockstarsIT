@model IEnumerable<SurveyViewModel>

@{
    ViewData["Title"] = "Enquête overzicht";
}

<link rel="stylesheet" href="~/css/Squads.css/Index.css" />

<h1>Enquête overzicht</h1>

<p>
    <a asp-action="Create" class="btn btn-primary btn-lg voeg-toe-button">Toevoegen</a>
   @*  <a asp-action="Create" class="btn btn-primary btn-lg">Toevoegen</a> *@
</p>

<div class="grid">
    @if (Model != null)
    {
        @foreach (var item in Model)
        {
            <div class="card">
                <button class="plus-button" aria-label="Add Item">+</button>
                <div class="card-header">
                    <h2>@item.Title</h2>
                </div>
                <div class="card-body">
                    <p>@item.Description</p>
                    <div class="actions">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Details</a>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <p>Geen Enquêtes gevonden.</p>
    }
</div>






@* @model IEnumerable<SurveyViewModel>

@{
    ViewData["Title"] = "Enquête overzicht";
}

<link rel="stylesheet" href="~/css/Survey.css/Index.css" />


<h1>Enquête overzicht</h1>

<p>
    <a>Maak een nieuwe enquête aan</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>

                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Details</a>
                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-warning btn-sm" })
                    @*<a asp-action="Details" asp-route-id="@item.Id">Bekijken</a> |*@
                    @* <a asp-action="Edit" asp-route-id="@item.Id">Bewerken</a> | *@
                    @* <a asp-action="Delete" asp-route-id="@item.Id">Verwijderen</a> *@
                @* </td>
            </tr>
        }
    </tbody>
</table>
 *@